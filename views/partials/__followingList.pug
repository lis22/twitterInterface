ul.app--user--list
    each friend in following
        li
            .circle--fluid
                .circle--fluid--cell.circle--fluid--primary
                    a.app--tweet--author
                        .app--avatar(style=`background-image: url(${friend.profilePic})`)
                            img(src=`${friend.profilePic}`)
                        h4 #{friend.fullName}
                        p @#{friend.screenName}
                .circle--fluid--cell
                    a.button.button-text Unfollow
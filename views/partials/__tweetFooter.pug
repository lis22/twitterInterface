.app--tweet
      form(action='/' method='POST')
        .circle--fluid
          .circle--fluid--cell.circle--fluid--primary.align-bottom.app--tweet--post
            .app--avatar(style=`background-image: url(${user.profilePic})`)
              img(src=`${user.profilePic}`)
            textarea#tweet-textarea.circle--textarea--input(placeholder="What's happening?", name="status", maxlength=140)
            strong#tweet-char.app--tweet--char 140
          .circle--fluid--cell.align-bottom
            button.button-primary(type='submit') Tweet
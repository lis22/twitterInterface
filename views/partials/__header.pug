header.circle--header(style=`background-image: url(${user.backgroundPic})`)
    .bounds
        .circle--fluid
          .circle--fluid--cell
            a.logo(href='/')
              svg(style=`background-image: url(${user.profilePic})`)
              h1 @#{user.screenName}
          .circle--fluid--cell.align-right
            nav
              ul.circle--inline
                li
                  a Sign Out